My backup of emacs configuration
--------------------------------


Copy .emacs into home directory
cp .emacs ~/.

Copy the lisp folder into .emacs.d folder

cp -r lisp ~/.emacs.d/.

Copy the snippets folder (my yasnippets definitions) into .emacs.d

cp -r snippets ~/.emacs.d/.

